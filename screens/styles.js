import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1, // Allow full height for web or mobile
    },
    safeArea: {
        flex: 1, // Allow full height for mobile
    },
    webContent: {
        flex: 1, // Allow full height for web
    },
  });

  export default styles;